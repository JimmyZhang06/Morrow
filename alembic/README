This directory contains the asynchronous Alembic environment.

The migration environment loads ``Settings`` from ``APP_*`` variables, imports
the explicit model registry, and uses ``life_coach.shared.database.Base`` as the
single metadata source. Production requires a non-loopback PostgreSQL host with
``ssl=verify-full``. Run with ``-c alembic/alembic.ini`` from the repository root.
