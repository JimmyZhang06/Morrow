# 设置、配色与页面切换

- 设置页统一使用 OpenAI 兼容 Chat Completions，删除协议下拉框和 JSON 模式复选框。已有 JSON 偏好保留，新配置无需理解这个开关。保留必要的地址、名称、模型、密钥和数据发送授权。
- 旧 Step Plan 后端适配器暂时保留以读取历史配置；设置页保存时统一写入 compatible。不改写用户现有档案、不删除已存密钥。
- 使用中国传统色灵感：宣纸白 `#F7F5EF`、黛青 `#285653`、朱砂 `#A64036`。原方案偏黄灰和草绿，导航与正文区分较弱；新方案以纸色承载阅读，黛青强化操作，朱砂仅作少量强调。此为项目自定义配色，不是国家认证色标。
- 移除整页透明淡入，滚动复位改为绘制前执行。记录当前页面和最近会话，重新打开时直接加载目标，不先显示今天页或新建对话。
- 异步认识生成和行动创建完成后，检查导航是否已改变，避免迟到结果强制跳走。
- TypeScript、Vite 构建通过。隔离桌面测试确认协议选项已移除、会话保留、刷新保留页面；记录页进入对话页连续 30 帧均只显示对话内容且无整页动画。
- 截图在 `.data/release-evidence/chinese-theme/`，已经人工视觉检查。
- 聊天回归通过：逐字输出、停止、中断正文保存、刷新后读取、EOF 最后帧和续聊。新版打包完成。

新版：`apps/desktop/release-theme-navigation/win-unpacked/Morrow.exe`。保留完整目录运行；本轮没有强制关闭用户旧版窗口。
